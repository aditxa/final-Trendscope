## Packages
framer-motion | Needed for smooth pipeline step animations and dashboard card reveals

## Notes
- Assume `@shared/schema` and `@shared/routes` export the provided schema and API manifest.
- The pipeline endpoint simulates a long-running process, so the frontend implements a visual step-by-step indicator during the mutation.
